{
  "name": "ovolar-block",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "snake:simulate": "tsc src/game/snake.ts src/game/snake-sim.ts --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir .tmp/snake-sim && node .tmp/snake-sim/snake-sim.js",
    "android:sync": "npm run build && npx cap sync android",
    "android:debug": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "typescript": "^5.8.3",
    "vite": "^7.1.7"
  }
}
